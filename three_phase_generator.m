% ID Number: 229,506
% ECE 31033 - Project #3
% three_phase_generator.m
% Problem #1

%% Initialization
param_init;

%% Calculations
%V_dc_M1 = (3 * sqrt(3) * E_amp / (4 * pi)) - (3 * w_ac * L_ac * i_dc/ pi);
%V_dc_M2 = 9 * E_amp * cos(alpha + (pi / 6)) / (2 * pi); 
%V_dc_M3 = 9 * E_amp * (1 - sin((pi / 6) + delta)) / (2 * pi);

% i_dc_M1 = 
%i_dc_M2 = (sqrt(3) * E_amp * sin(alpha + (pi / 6))) / (2 * w_ac * L_ac);
%i_dc_M3 = (1 - sin(delta + (pi / 6))) * E_amp / (2 * w_ac * L_ac);

